/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
         // Supabaseのドメインを追加
        domains: ['mjpiymqijkcwavtgjssq.supabase.co'],
      },
};

export default nextConfig;
